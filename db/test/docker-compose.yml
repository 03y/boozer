services:
  db:
    container_name: boozer_test_db
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: boozer-test
    ports:
      - 5001:5432
    # no volumes, data will not persist
